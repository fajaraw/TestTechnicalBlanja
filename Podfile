# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestBlanja' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 4.7’
  pod 'SwiftyJSON', '~> 4.0'
  pod 'SDWebImage', '~> 4.0'
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'DateToolsSwift'



  target 'TestBlanjaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestBlanjaUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
